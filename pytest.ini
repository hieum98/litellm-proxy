[pytest]
# Pytest configuration for Redis cache tests

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    -v
    --tb=short
    --strict-markers

# Markers
markers =
    redis: tests that require Redis connection
    slow: tests that take a long time to run

